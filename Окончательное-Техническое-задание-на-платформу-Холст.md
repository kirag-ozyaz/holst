# 🎨 **Окончательное техническое задание: Платформа «Холст»**
*Визуальная среда для задач, заметок, связей и знаний*

---

## 🔷 **1. Общее описание**

Платформа «Холст» — это **веб-приложение с бесконечным холстом**, в котором пользователь может:
- Создавать и управлять **задачами** и **подзадачами** (в виде квадратиков, сворачиваемых/разворачиваемых).
- Создавать **свободные заметки** и **привязывать их к задачам**.
- Строить **двунаправленные связи между заметками** и **направленные связи между задачами** (с защитой от зацикливания).
- Визуализировать всю сеть связей в виде **интерактивного графа**.
- Работать с **таблицами**, **изображениями**, **файлами** внутри карточек и заметок.
- Использовать **голосовой ввод**, **поиск**, **экспорт в PDF/Markdown**, **ИИ-генерацию**.
- Вся система работает **локально**, без подключения к интернету при запуске.

---

## 🔧 **2. Технологический стек**

| Компонент | Технология |
|----------|------------|
| Backend | **FastAPI** (Python 3.10+) |
| База данных | **PostgreSQL** |
| ORM | **SQLAlchemy** |
| Frontend | **Vue.js 3** + **Pinia** |
| UI | **Tailwind CSS** + **Headless UI** |
| Canvas | **Konva.js** (локально) |
| Граф связей | **Cytoscape.js** (локально) |
| Редактор | **Tiptap** (локально) |
| Перетаскивание | **interact.js** |
| STT (голос) | **Vosk** (оффлайн, русский язык) |
| PDF-экспорт | **WeasyPrint** |
| Веб-сервер | **Nginx** |
| WSGI-сервер | **Gunicorn** |
| Контейнеризация | **Docker** + **Docker Compose** |

> ⚠️ **Все зависимости предварительно скачаны и включены в проект. Сборка и запуск без интернета.**

---

## 📦 **3. Функциональные требования**

### ✅ **3.1. Холст (Canvas)**
- Бесконечное пространство с масштабированием и панорамированием.
- Поддержка сетки и привязки.
- Адаптивность: десктоп + мобильные устройства.

### ✅ **3.2. Задачи и подзадачи**
- Карточки с иерархией (любая вложенность).
- Подзадачи отображаются как **квадратики**, сворачиваются/разворачиваются.
- Перетаскивание внутри холста, сохранение позиции.

### ✅ **3.3. Заметки**
- Свободные заметки на холсте.
- Привязка заметок к задачам.
- WYSIWYG-редактор с поддержкой текста, таблиц, изображений.

### ✅ **3.4. Связи**
- **Между задачами**: `depends_on`, `blocks`, `follows`, `related_to` — с **контролем зацикливания**.
- **Между заметками**: **двунаправленные связи** (`linked_to`).
- Визуализация линиями на холсте.

### ✅ **3.5. Граф связей**
- Отдельный режим «Граф».
- Отображение всех узлов (задачи, заметки) и рёбер (связи).
- Интерактивность: масштаб, перетаскивание, фильтрация, переход на холст.

### ✅ **3.6. Файлы**
- Загрузка файлов (до 20 МБ) к карточкам/заметкам.
- Хранение в volume (`/data/uploads/...`).
- Просмотр изображений по двойному клику.
- Безопасный доступ через API (не через Nginx напрямую).

### ✅ **3.7. Поиск**
- Единое поле поиска по всему контенту.
- Full-text search через PostgreSQL (`tsvector`).
- Переход к найденному элементу на холсте.

### ✅ **3.8. Экспорт**
- Экспорт заметок и задач в:
  - **Markdown** (сохранение структуры).
  - **PDF** (визуальное соответствие отображению).

### ✅ **3.9. Голосовой ввод**
- Кнопка 🎤 в редакторе.
- Использование **Vosk** для распознавания речи (русский язык).
- Вставка результата в заметку или отправка как ИИ-запрос.

### ✅ **3.10. ИИ-интеграция (опционально)**
- Генерация контента по запросу (текст, таблицы, задачи).
- Сохранение результата как заметки или карточки.

### ✅ **3.11. История изменений**
- Логирование всех действий в `event_log`.
- Поддержка **undo/redo** (Ctrl+Z / Ctrl+Y).
- Просмотр истории по карточке или холсту.

---

## 📡 **4. API (основные эндпоинты)**

```http
# Холст
GET    /api/canvas

# Задачи
POST   /api/cards
PUT    /api/cards/{id}
DELETE /api/cards/{id}

# Подзадачи — через cards (рекурсивно)

# Связи задач
POST   /api/task-links
DELETE /api/task-links/{id}

# Заметки
POST   /api/notes
PUT    /api/notes/{id}
GET    /api/notes/{id}/export?format=markdown|pdf

# Связи заметок
POST   /api/note-links
DELETE /api/note-links/{id}

# Файлы
POST   /api/cards/{id}/files
GET    /api/files/{id}/download

# Поиск
GET    /api/search?q=...

# Голос
POST   /api/voice/transcribe  → принимает аудио → возвращает текст

# Граф
GET    /api/graph  → возвращает структуру узлов и рёбер

# История
POST   /api/history/undo
POST   /api/history/redo
```

---

## 🗃️ **5. Архитектура проекта**

```
/canvas-platform/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── models/          # Card, Note, File, Link, EventLog
│   │   ├── schemas/
│   │   ├── crud/
│   │   ├── services/        # search, export, voice, cycle_detection
│   │   └── database.py
│   ├── requirements.txt
│   └── Dockerfile.gunicorn
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Canvas.vue
│   │   │   ├── Card.vue
│   │   │   ├── Note.vue
│   │   │   ├── GraphView.vue
│   │   │   └── Toolbar.vue
│   │   ├── stores/          # canvasStore, searchStore, etc.
│   │   └── main.js
│   ├── public/libs/         # локальные копии Konva, Cytoscape, Tiptap, Vosk
│   └── Dockerfile.build
├── voice-stt/               # контейнер с Vosk
│   └── Dockerfile
├── nginx/
│   └── nginx.conf
├── docker-compose.yml
└── README.md
```

---

## 🚀 **6. Пошаговый план выполнения работы**

### **Этап 1. Подготовка инфраструктуры**
- Настроить структуру проекта.
- Создать `docker-compose.yml` с сервисами: `backend`, `frontend`, `db`, `nginx`, `voice-stt`.
- Настроить volume для `/data` (файлы).
- Убедиться, что все зависимости (pip, npm) закэшированы локально.

### **Этап 2. База данных и модели**
- Создать модели: `Card`, `Note`, `File`, `TaskLink`, `NoteLink`, `EventLog`.
- Реализовать миграции (Alembic).
- Настроить full-text search для `Card` и `Note`.

### **Этап 3. Бэкенд API**
- Реализовать CRUD для карточек, заметок, файлов.
- Добавить эндпоинты для связей (с проверкой зацикливания).
- Реализовать поиск, экспорт (Markdown), историю изменений.
- Интегрировать Vosk (голосовой ввод).

### **Этап 4. Холст и редактор (фронтенд)**
- Реализовать бесконечный холст на Konva.js.
- Добавить создание/редактирование карточек и заметок.
- Встроить Tiptap-редактор с поддержкой таблиц и изображений.
- Реализовать перетаскивание, масштабирование, сворачивание подзадач.

### **Этап 5. Связи и граф**
- Реализовать визуальные связи на холсте.
- Создать отдельный компонент **GraphView** на Cytoscape.js.
- Синхронизировать данные между холстом и графом.

### **Этап 6. Дополнительные функции**
- Добавить поиск с переходом на элемент.
- Реализовать экспорт в PDF (WeasyPrint).
- Интегрировать голосовой ввод (кнопка 🎤 → Vosk → вставка текста).
- Добавить undo/redo через историю.

### **Этап 7. Адаптивность и UX**
- Настроить адаптивный интерфейс для мобильных устройств.
- Оптимизировать touch-взаимодействие.
- Добавить панель инструментов, меню `+`, контекстные меню.

### **Этап 8. Тестирование и документация**
- Проверить работу без интернета.
- Протестировать все сценарии: создание, связывание, экспорт, голос, поиск.
- Написать `README.md` с инструкцией по запуску, API, архитектуре.

---

## ✅ **7. Критерии готовности (Definition of Done)**

- [ ] Холст с задачами, подзадачами, заметками.
- [ ] Двунаправленные связи между заметками.
- [ ] Связи между задачами с защитой от зацикливания.
- [ ] Графовая визуализация всей сети.
- [ ] Поиск по всему контенту.
- [ ] Экспорт в Markdown и PDF.
- [ ] Голосовой ввод (русский, Vosk).
- [ ] Файлы: загрузка, просмотр, безопасность.
- [ ] История изменений + undo/redo.
- [ ] Адаптивность (мобильные устройства).
- [ ] Работа через Nginx + Gunicorn в Docker.
- [ ] Все зависимости локальные — сборка без интернета.
- [ ] Полная документация в `README.md`.

---

## 💡 **8. Дальнейшее развитие**

- Теги и фильтрация.
- Совместная работа (real-time через WebSocket).
- Автоматическое резервное копирование холста.
- Интеграция с внешними ИИ (Ollama, LM Studio) для локального LLM.

---

✅ **Готово к разработке.**  
Платформа «Холст» объединяет планирование, управление знаниями и визуальное мышление в единой, автономной, интерактивной среде.