# trash/

Здесь хранятся удалённые/устаревшие файлы, которые могут понадобиться для справки.

## crud/

Мёртвый CRUD-модуль. Содержал базовый класс `BaseCRUD` и специализированные классы `TaskCRUD` и `NoteCRUD`.

**Почему удалён**: Эндпоинты в `main.py` выполняли CRUD-операции напрямую через `db.query()`, а не через эти классы. Код никогда не использовался.

## services/

Пустой каталог для сервисного слоя.

**Почему удалён**: Не содержал кода.
